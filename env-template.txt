# ============================================
# DSGVO-konforme LLM-Plattformen Vergleich
# Umgebungsvariablen für Coolify Deployment
# ============================================
# Kopieren Sie diese Datei und benennen Sie sie in .env um
# oder fügen Sie die Variablen in Coolify ein

# ============================================
# DATENBANK (MySQL) - ERFORDERLICH
# ============================================
DATABASE_URL=mysql://username:password@your-mysql-host:3306/llm_platform

# ============================================
# SERVER KONFIGURATION - ERFORDERLICH
# ============================================
NODE_ENV=production
PORT=3000
SITE_URL=https://ihre-domain.de

# ============================================
# SICHERHEIT - ERFORDERLICH
# ============================================
# Generieren mit: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# ============================================
# E-MAIL KONFIGURATION (SMTP) - ERFORDERLICH für Magic-Link-Login
# ============================================
# Ohne SMTP funktioniert das Magic-Link-Login nicht!
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
ADMIN_EMAIL=admin@ihre-domain.de
FROM_EMAIL=noreply@ihre-domain.de

# ============================================
# FRONTEND - OPTIONAL
# ============================================
VITE_APP_TITLE=DSGVO-konforme LLM-Plattformen Vergleich
VITE_APP_LOGO=

# ============================================
# HINWEIS: SELF-HOSTED MODUS
# ============================================
# Diese App läuft im Self-Hosted-Modus ohne OAuth.
# Das Login erfolgt ausschließlich über Magic-Link (E-Mail).
#
# Um einen Admin-Benutzer zu erstellen:
# 1. Loggen Sie sich über Magic-Link ein
# 2. Führen Sie in der Datenbank aus:
#    UPDATE members SET role = 'admin' WHERE email = 'ihre-email@domain.de';
#
# Die folgenden Manus-spezifischen Variablen sind NICHT erforderlich:
# OAUTH_SERVER_URL, VITE_APP_ID, OWNER_OPEN_ID, etc.
